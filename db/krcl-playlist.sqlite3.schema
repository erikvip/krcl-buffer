CREATE TABLE playlist(id integer primary key not null, start datetime, end datetime, artist text, release text, song text, duration integer, showtitle text, songindex integer default null, streamfile text default null);
CREATE TABLE shows (showtitle text unique, save integer default 0);
